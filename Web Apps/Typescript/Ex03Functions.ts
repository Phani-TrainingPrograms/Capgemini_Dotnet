export function addFunc(f : number, s : number) : number{
    return f + s;
}

export function subFunc(f:number, s: number): number{
    return f -s;
}

const res = addFunc(123, 23);
console.log(res);